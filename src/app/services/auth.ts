export class Profile {
    key: string;
    id: string;
    nama: string;
    phone: string;
    email: string;
    constructor(key: string, id: string, nama: string, phone: string, email: string) {}
}
